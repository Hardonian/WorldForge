[workspace]
resolver = "2"
members = [
    "crates/worldforge-core",
    "crates/worldforge-ecs",
    "crates/worldforge-world",
    "crates/worldforge-runtime",
    "crates/worldforge-replay",
    "crates/worldforge-package",
    "crates/worldforge-mod-api",
    "crates/worldforge-mod-runtime",
    "crates/worldforge-economy",
    "crates/worldforge-agent",
    "crates/worldforge-proof",
    "crates/worldforge-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/Hardonian/WorldForge"
rust-version = "1.75"

[workspace.dependencies]
# Internal crates
worldforge-core = { path = "crates/worldforge-core" }
worldforge-ecs = { path = "crates/worldforge-ecs" }
worldforge-world = { path = "crates/worldforge-world" }
worldforge-runtime = { path = "crates/worldforge-runtime" }
worldforge-replay = { path = "crates/worldforge-replay" }
worldforge-package = { path = "crates/worldforge-package" }
worldforge-mod-api = { path = "crates/worldforge-mod-api" }
worldforge-mod-runtime = { path = "crates/worldforge-mod-runtime" }
worldforge-economy = { path = "crates/worldforge-economy" }
worldforge-agent = { path = "crates/worldforge-agent" }
worldforge-proof = { path = "crates/worldforge-proof" }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ciborium = "0.2"
toml = "0.8"

# Hashing
blake3 = "1"

# RNG
rand = "0.8"
rand_chacha = "0.3"

# CLI
clap = { version = "4", features = ["derive"] }

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Errors
thiserror = "1"

# Versioning
semver = { version = "1", features = ["serde"] }

# Time (metadata only)
chrono = { version = "0.4", features = ["serde", "clock"], default-features = false }

# Archive
tar = "0.4"

# Testing
criterion = { version = "0.5", features = ["html_reports"] }

# Misc
uuid = { version = "1", features = ["v4", "serde"] }
